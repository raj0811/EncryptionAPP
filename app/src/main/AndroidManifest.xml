<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.raj.encryptionapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EncryptionApp">
        <activity
            android:name=".morseChart"
            android:label="morsechart"
            android:parentActivityName=".morsecode" />
        <activity
            android:name=".morsecode"
            android:label="morseActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".enc"
            android:label="Second Activity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>